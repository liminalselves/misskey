# 智能体后台整合开发文档

## 目标

将控制台内分散的智能体后台能力整合为两个入口：

- 管理 -> 智能体审查：面向拥有审查权限的用户，负责内容审核、会话审计、封禁处置与审查记录。
- 设置 -> 智能体管理：面向拥有管理权限的用户，负责平台配置、模型、记忆、压缩、额度兑换码与报表。

## 权限口径

- 智能体审查入口不应仅限站点管理员。审查列表、审查处理、会话审计、角色/会话封禁与审查记录应允许拥有管理/审核权限的账号访问。
- 智能体管理入口仍属于设置能力。模型、额度、全局提示词、记忆、压缩与报表应允许拥有管理权限的账号访问。
- 后端 API 需要从纯 `requireAdmin` 逐步调整为更贴合入口的权限模型，避免前端入口可见但 API 不可用。

## 页面整合

### 智能体审查

整合并增强以下能力：

- 待审角色与风格提示词列表。
- 角色/风格完整详情查看。
- 所有角色、所有风格提示词检索与处置。
- 拒绝审核时填写标准原因、面向作者的说明、内部备注。
- 会话审计与消息检索。
- 角色封禁/解封、会话封禁/解封。
- 智能体相关审查记录与处置日志。

### 智能体管理

整合并增强以下能力：

- 智能体基础设置。
- 模型配置与默认模型。
- 长期记忆配置。
- 压缩配置。
- 兑换码生成、列表、撤销。
- 请求报表与成本/成功率统计。
- 平台总览。

## 后端 API 调整

需要补齐或调整：

- 审查列表支持队列筛选、分页、所有内容检索。
- 审查处理支持拒绝原因与内部备注，并写入审查日志。
- 会话审计 API 从管理员限定调整为审查权限可用。
- 兑换码与报表 API 保持管理权限可用。
- 角色/风格封禁处置支持记录原因。
- 完成产品设计中的增强项：风险提示、差异/原文/预览辅助视图、审核模板、管理总览、模型健康提示、额度批次视图、运营报表摘要。

## 前端调整

- 控制台菜单只保留两个智能体入口：智能体审查、智能体管理。
- 智能体审查页面改为多标签工作台。
- 智能体管理页面改为多标签工作台，并吸收原报表与兑换码页面能力。
- 保留旧路由的兼容跳转，避免已有链接失效。
- 新增中文文案，必要时补齐英文/日文 fallback。
- 审查工作台必须支持大型社区常见操作：检索、筛选、批量浏览、封禁理由、证据复制、作者跳转、历史审查信息可见。

## 验收

- 管理分组只出现智能体审查入口。
- 设置分组只出现智能体管理入口。
- 审查页能查看待审角色、待审风格、所有角色、所有风格、会话审计、审查记录。
- 拒绝审核必须输入原因。
- 管理页能完成原智能体设置、报表、兑换码管理的主要功能。
- 后端与前端类型检查通过。
