# 智能体 + 长期记忆开发说明（阿里云百炼记忆库）

写给「先对齐怎么再做」用。不讲代码细节，只讲**要做什么、先后顺序、你要先决定什么**。

**官方文档（必读，以官网为准）：** [长期记忆（新）API 参考 - 大模型服务平台百炼](https://help.aliyun.com/zh/model-studio/long-term-memory-2-0)

---

## 0. 已决议（避免以后改口）

### 0.1 供应商

| 项目 | 结论 |
|------|------|
| 记忆服务 | **阿里云百炼「长期记忆（新）」** |
| 与 Mem0 | 不采用；HTTP 对接百炼 |

定价与「限时免费」等以阿里云控制台与文档当期说明为准。

### 0.2 产品行为（已拍板）

| # | 决议 | 说明 |
|---|------|------|
| 1 | **记忆按「会话」隔离** | 每个智能体会话一套记忆，不与其他会话混用。实现上建议百炼侧 `user_id`（或等价维度）**绑定 sessionId**（例如 `misskeyUserId + ':' + sessionId`），保证 Add / Search 成对一致。 |
| 2 | **每轮结束后 AddMemory** | 该轮 user + assistant 成功后 **异步** 调用 add（不阻塞返回）；注意官方 **add QPM** 上限。 |
| 3 | **每轮发消息前 SearchMemory** | 每次用户发送、调用自有 LLM **之前**检索；`top_k` 等可由会话内参数控制（见下）。 |
| 4 | **挂了：提示 + 正常聊** | 搜索/写入失败：**不中断对话**、不 500；对用户 **显示一条轻量提示**（如 Toast / 顶栏条：「长期记忆暂不可用，本轮未注入记忆」），后台记日志。 |
| 5 | **密钥只在管理端** | `DASHSCOPE_API_KEY`、默认 `memory_library_id` 等 **仅控制面板 → 智能体相关设置**；**不得**出现在会话「记忆」子界面。 |

### 0.3 界面分工

| 位置 | 放什么 |
|------|--------|
| **管理端 · 智能体设置** | 记忆总开关、百炼 API Key、默认记忆库 ID 等敏感/全站配置 |
| **会话界面 · 子选项「记忆」** | **仅面向用户** 的参数：例如本会话是否启用记忆、检索条数上限（`top_k`）、注入总字数上限、`min_score`（若开放）等；**不写密钥** |

会话内参数存 **按会话**（DB：`agent_session` 扩展字段或关联小表），后端发消息时与会话 id 一起参与拼 `user_id` / 请求体。

---

## 1. 这份文档是干什么的

你们现在的智能体聊天：**把最近对话 + 角色设定塞进模型，模型回一句，存库**。  
加上长期记忆后：**多一个「云端记事本」**，能从历史对话里**自动提炼**记忆节点，并在新对话里**按语义搜**出相关片段，塞进本次 Prompt，让体验更连贯。

本文说明：**在现有 Misskey 智能体上怎么接这套 API**，以及开发顺序。

---

## 2. 现在系统里有什么（极简版）

| 东西 | 干什么 |
|------|--------|
| 会话 | 用户和某个角色聊天的一条线 |
| 消息表 | 存 user / assistant 的**原文**，一条一条 |
| 发消息接口 | 读最近消息 → 拼系统提示 → 调 LLM → 把助手回复再存进去 |
| 上下文上限 | 最近消息不能无限长，超过就**截断**（像只保留尾巴的一卷纸） |

**没有**：按会话隔离的云端提炼记忆、语义检索（由百炼 + 本节规划补上）。

---

## 3. 阿里云记忆库在流程里干什么

- **不是**替换「最近几轮对话」：最近聊了什么，模型仍应看到**原文**才自然。  
- **是**在窗口之外：**AddMemory** 把一轮对话交给服务端提炼；**SearchMemory** 按当前语境语义检索，把命中的 `content` 注入系统提示或单独小节。

**与 0.2 对齐：** 每个 Misskey **会话** 在百炼侧视为独立「用户线」，避免 A 会话记忆灌进 B 会话。

---

## 4. 对接时要记住的 API 要点（从官方文档摘的硬信息）

**鉴权：** `Authorization: Bearer <DASHSCOPE_API_KEY>`（**仅服务端**读取管理端配置，勿下发前端）。

**和本集成最相关的两个动作：**

| 动作 | 方法 | 路径（均在 `https://dashscope.aliyuncs.com` 下） | 用途 |
|------|------|--------------------------------------------------|------|
| 添加记忆 | POST | `/api/v2/apps/memory/add` | 每轮结束后 `user_id`（建议含 sessionId）+ 本轮 `messages` |
| 搜索记忆 | POST | `/api/v2/apps/memory/memory_nodes/search` | 发消息前 `user_id` + 当前 `messages`，`top_k` 等 |

**常用可选参数：** `memory_library_id`（管理端默认）、`meta_data`（可选打标）、`top_k` / `min_score`（可与会话内用户参数绑定）。

**限流（以官网为准）：** 全接口约 **3000 QPM**；**add** 约 **120 QPM**，**search** 约 **300 QPM** → **Add 必须异步**；全站注意合并与队列。

---

## 5. ~~动手前仍要拍板~~ → 已由 §0.2 定案

若后续变更（例如改为「按用户全局记忆」），只改 **§0.2** 并在此留一行变更记录即可。

---

## 6. 建议开发顺序（按步做，别跳）

### 第一步：管理端配置

- 智能体设置页：**总开关**、**API Key**、可选默认 **memory_library_id**；默认关闭。

### 第二步：会话级数据与 API

- 为 `agent_session`（或等价）增加记忆相关字段，或子表：**本会话是否启用**、**top_k**、注入长度上限等。  
- 提供 **用户可调的** 会话设置 API（读写在会话成员本人权限内）。

### 第三步：会话 UI ·「记忆」子选项

- 在单个会话界面增加 Tab / 子页 **「记忆」**：只渲染 §0.3 中「面向用户」项；保存时调上一步 API。

### 第四步：读路径（SearchMemory）

- 发消息前：若总开关开 **且** 本会话启用 → Search；失败 → **提示文案 + 不带记忆继续**。  
- `user_id` 与 Add 一致（含 session 维度）。

### 第五步：写路径（AddMemory）

- 助手回复落库成功后 **异步** Add；失败 → **仅日志**（可选轻提示，避免刷屏可合并为「本轮记忆未保存」类）。

### 第六步：观测与限流

- 日志 + 429 处理；必要时队列化 add。

---

## 7. 和现有代码的大致关系（方便拆任务）

| 方向 | 大概会动哪里 |
|------|----------------|
| 管理端密钥 / 总开关 | `meta` + `admin/update-meta` + `agents-settings`（或独立折叠块） |
| 会话记忆参数 | `agent_session` 模型与迁移、会话更新 API、前端会话页「记忆」子选项 |
| 搜索 + 注入 + 失败提示 | 智能体 `messages/send`（或抽 `AgentMemoryService`） |
| 异步 Add | 同上，助手消息写入后 `setImmediate` / 任务队列 |

---

## 8. 语言与内容形态

百炼示例里记忆 `content` 多为中文提炼；以实际上线效果为准。

---

## 9. 验收标准（草履虫版）

- 总开关关：与现在行为一致。  
- 本会话关记忆：不搜不写，行为与无记忆一致。  
- 本会话开记忆：多轮后能用到本会话早期信息。  
- 百炼挂了：**仍能正常对话**，且用户**看得到**「记忆不可用」类提示（§0.2 #4）。  
- 密钥：**只**出现在管理端，会话「记忆」页**没有**密钥输入框。

---

## 10. 文档维护

- API 变更以 [官方长期记忆文档](https://help.aliyun.com/zh/model-studio/long-term-memory-2-0) 为准。  
- 产品决议变更只改 **§0**。

---

*本文档位于仓库根目录，仅描述本 fork 的规划意图，与上游 Misskey 官方文档无关。*
