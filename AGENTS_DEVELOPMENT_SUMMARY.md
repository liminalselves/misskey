# Misskey 智能体功能开发总览文档

本文档用于汇总当前 Misskey 仓库中「智能体 / Agents」相关功能的总体设计、已落地实现、当前开发方向、后续扩展项与协作约定。它的目标不是替代 PRD，而是让后续开发者在打开仓库根目录时，就能快速理解：

- 这套系统要解决什么问题
- 当前已经做到哪里
- 接下来应该怎么继续
- 哪些约束不能破

---

## 1. 项目背景

Misskey 当前的智能体功能并不是单纯的聊天机器人，而是一套围绕「角色设定」与「对话风格」展开的站内创作与游玩系统。

它的核心产品形态可以理解为：

- 用户创建角色设定：角色是谁、有什么背景、怎么说话、有什么禁忌
- 用户创建对话风格：这次聊天要更偏日常、剧情、旁白、短句、长句等
- 用户启动会话：把角色和风格组合到一起，开始聊天
- 作者可测试、发布、管理版本、回滚
- 普通用户只接触发布态内容

该功能全部为**站内闭环**，不走联邦协议，不与远端实例同步。

---

## 2. 已确认的产品目标

### 2.1 核心目标

1. 让用户能够创建和管理角色设定。
2. 让用户能够创建和管理对话风格。
3. 让用户通过会话把角色与风格绑定起来聊天。
4. 让角色作者能够在草稿状态下测试自己的角色。
5. 让发布态角色可被社区用户使用。
6. 提供版本管理能力，支持草稿、发布、历史快照与回滚。
7. 支持世界书功能：角色专属背景知识与触发规则。
8. 让编辑界面保持 Misskey 的风格，同时更高效。

### 2.2 已拍板的边界

- **不做联邦化**
- **不做用户自带 API Key 直连模型**
- **不做扣费 / 余额 / 商业闭环首版**
- **不做每日上线次数硬限制**
- **只在本实例内闭环**

### 2.3 会话模型

- 会话绑定：角色 + 对话风格 + 当前会话上下文
- 同一用户同一角色可以有多个会话
- 会话可重命名
- 测试会话与正式会话使用同一套数据结构，只是角色来源不同

---

## 3. 当前系统拆分

智能体系统在仓库中主要分为四层：

1. **数据库实体层**
2. **后端服务与 API 层**
3. **前端编辑与游玩层**
4. **管理配置与 Meta 层**

---

## 4. 数据模型总览

### 4.1 角色实体 `agent_character`

角色是系统最重要的对象，主要字段包括：

- `id`
- `userId`
- `name`
- `summary`
- `personality`
- `background`
- `speakingStyle`
- `greeting`
- `exampleDialogue`
- `forbiddenBehavior`
- `avatarFileId`
- `isPublished`
- `reviewStatus`
- `publishedVersion`
- `publishedSnapshot`
- `promptOpenSourced`

#### 当前新增方向

角色实体已经开始承载：

- `worldbook`
- `draftRevision`

这意味着角色不再只是“若干字段的集合”，而是逐步变成“完整可版本化资产”。

### 4.2 对话风格实体 `agent_dialogue_style`

风格实体包括：

- `id`
- `userId`
- `name`
- `body`
- `summary`
- `isPublished`
- `reviewStatus`
- `publishedVersion`
- `publishedSnapshot`
- `promptOpenSourced`

#### 当前新增方向

风格实体已经补上：

- `draftRevision`

即风格也进入了草稿 / 发布 / 快照的统一轨道。

### 4.3 会话实体 `agent_session`

会话用于承载聊天上下文，并绑定：

- 所有者
- 角色
- 风格
- 会话类型（测试 / 社区）
- 最近消息时间
- 是否正在生成回复

### 4.4 消息实体 `agent_message`

消息负责保存用户与助手的交互内容。

基本内容：

- `sessionId`
- `role`
- `content`
- `createdAt`

可扩展内容：

- `promptTokens`
- `completionTokens`

---

## 5. 版本管理策略

当前版本管理正在从“字段快照”升级为“整角色资产快照”。

### 5.1 角色版本管理

角色版本管理应该覆盖：

- 基础信息
- 人设
- 背景
- 对话风格描述
- 开场白
- 示例对话
- 禁止行为
- 世界书
- 头像

#### 当前已在做的事

- 引入 `draftRevision`
- `publish` 时保存 `publishedSnapshot`
- `show` 接口可返回版本相关信息
- 编辑页预留了版本管理入口

### 5.2 对话风格版本管理

对话风格也需要版本化，避免作者改坏风格后无法恢复。

#### 当前已在做的事

- 增加 `draftRevision`
- `publish` 时保存 `publishedSnapshot`
- `unpublish` 状态流转整理

### 5.3 版本状态建议

建议维持以下状态语义：

- `draft`：草稿中
- `pending`：提交审核 / 待发布
- `published`：已发布
- `rejected`：被拒绝

### 5.4 版本操作建议

后续版本管理页最好支持：

- 查看当前草稿
- 查看历史版本
- 从历史版本回滚为新草稿
- 对比两个版本差异
- 标注版本备注

---

## 6. 世界书设计

### 6.1 世界书是什么

世界书是角色专属的背景知识库，可用于：

- 世界观基础设定
- 人物关系
- 历史事件
- 关键名词解释
- 常驻规则
- 临时剧情注入

### 6.2 当前模型方向

世界书目前以角色内嵌 JSON 的方式承载，第一版先重速度与可用性，后续再考虑拆表。

一个世界书条目大致应包含：

- `id`
- `title`
- `content`
- `keywords`
- `triggerMode`
- `priority`
- `enabled`
- `revision`

### 6.3 触发器策略

已规划的触发方式：

- `keyword`：关键词触发
- `manual`：手动触发
- `always`：常驻注入

后续可扩展：

- `semantic`：语义检索触发
- `context`：上下文规则触发

### 6.4 当前落地情况

当前已经做到：

- 角色编辑页中可以直接编辑世界书条目
- 支持新增 / 删除 / 复制
- 支持标题 / 内容 / 关键词 / 触发模式 / 优先级 / 启用状态
- 保存时会随角色一起提交
- 系统提示词拼装已经开始接入世界书

### 6.5 运行时接入方向

世界书在运行时会进入模型上下文，原则是：

- 启用条目才参与注入
- 常驻条目优先
- 触发条目按规则选择
- 结构化输出，避免污染用户消息

---

## 7. 提示词分层

当前系统采用逻辑分层，而不是简单拼一坨文本。

### 7.1 层级结构

1. **全局设定**
   - 平台级规则
   - 管理员配置
   - 安全与行为约束

2. **角色设定**
   - 角色是谁
   - 什么性格
   - 什么背景
   - 怎么说话
   - 什么禁忌

3. **世界书**
   - 世界观知识
   - 关键词 / 常驻 / 手动触发内容

4. **对话风格**
   - 这一轮如何表达
   - 语气、格式、长度、旁白等

### 7.2 当前拼装方式

- `buildSystemPrompt(...)` 负责系统提示词主结构
- `buildLatestUserDirectiveBlock(...)` 负责把对白风格注入到最新用户消息前
- `wrapLatestUserTextWithStyleDirective(...)` 负责最终拼接
- `send` 接口已经在真实对话链路中使用它们

### 7.3 设计原则

- 风格指令不落库，不进长期记忆
- 世界书只作为模型可见上下文，不污染原始用户输入
- 示例对话是参考，不是已发生对话
- 所有可控文本都要做 XML 转义，避免结构破坏

---

## 8. 后端 API 总览

### 8.1 角色相关

- `agents/characters/create`
- `agents/characters/update`
- `agents/characters/delete`
- `agents/characters/show`
- `agents/characters/list-mine`
- `agents/characters/public-list`
- `agents/characters/publish`
- `agents/characters/unpublish`

### 8.2 风格相关

- `agents/styles/create`
- `agents/styles/update`
- `agents/styles/delete`
- `agents/styles/show`
- `agents/styles/list-mine`
- `agents/styles/public-list`
- `agents/styles/publish`
- `agents/styles/unpublish`

### 8.3 会话与消息

- `agents/sessions/create`
- `agents/sessions/update`
- `agents/sessions/delete`
- `agents/sessions/show`
- `agents/sessions/list-mine`
- `agents/messages/timeline`
- `agents/messages/send`
- `agents/messages/abort`

### 8.4 当前调用链重点

发送消息时，后端会：

1. 校验会话归属与状态
2. 读取角色与风格
3. 构造 system prompt
4. 构造风格 directive
5. 调用 OpenAI 兼容接口
6. 写入 assistant 消息
7. 处理长期记忆 / 压缩侧车

---

## 9. Meta / 管理配置

### 9.1 功能开关

- `agentFeatureEnabled`

### 9.2 模型与接口配置

- OpenAI 兼容 Base URL
- API Key
- 模型展示名称
- 模型说明
- 模型 API 名称
- 最大上下文 token
- 单次最大输出 token
- 全局 system prompt

### 9.3 安全要求

- Base URL 必须做 HTTPS / SSRF 校验
- API Key 只允许管理员写入
- 不向普通用户暴露密钥
- 任何模型请求都必须走服务端代理

---

## 10. 前端结构

### 10.1 ` /agents ` 主入口

当前 `/agents` 页面承担角色与风格管理入口。

推荐结构：

- 角色设定 Tab
- 对话风格 Tab
- 我的统计 Tab

### 10.2 角色编辑页

`character-edit.vue` 已被重构为更像创作工作台的布局：

- 顶部快捷跳转
- 基础信息
- 人设
- 对话内容
- 世界书
- 安全约束
- 版本管理
- 右侧实时预览

### 10.3 风格编辑页

`style-edit.vue` 也已向同样风格靠拢：

- 更简洁的编辑结构
- 预留版本入口
- 实时预览

### 10.4 对话页

对话页目前已经可以按会话游玩，并与发送链路联动。

### 10.5 页面风格约束

前端改造原则：

- 保持 Misskey 的卡片感、简洁感、克制感
- 用抽屉 / 局部编辑优于跳页
- 用预览减少来回切换
- 用较少层级让创作者操作更快

---

## 11. 已落地实现进度

以下内容表示当前仓库中已经有明显落地的部分。

### 11.1 已完成方向

- 智能体功能总开关
- 管理端模型配置
- 角色 CRUD
- 风格 CRUD
- 会话创建
- 消息发送
- 角色 / 风格发布与取消发布
- 角色 / 风格版本快照方向
- 角色 / 风格版本列表接口
- 世界书编辑结构
- 对话风格运行时注入
- 角色系统提示词拼装
- 角色版本列表接口
- 风格版本列表接口
- 角色编辑 UI 重构
- 风格编辑 UI 重构

### 11.2 当前仍在继续推进的方向

- 版本差异对比
- 回滚到历史版本的前端确认与展示
- 世界书触发器命中逻辑
- 世界书运行时选择策略
- 更完整的编辑器交互细节

---

## 12. 当前工作流约定

### 12.1 开发优先级

建议按以下顺序继续：

1. 版本管理后端接口补齐
2. 世界书触发器后端逻辑补齐
3. 编辑页接入可操作版本列表
4. 编辑页接入世界书命中状态
5. 运行时对话链路继续优化

### 12.2 编码原则

- 尽量优先编辑已有文件，不轻易新增文件
- 保持代码风格和仓库现有结构一致
- 后端 API 与前端页面要同步考虑
- 任何能在现有模型上做的扩展，优先不推翻已有结构

### 12.3 安全与兼容性

- 保持输出 / 上下文 / token 控制
- 不让普通用户看到管理员配置
- 不让草稿泄漏到发布态列表
- 不让未授权用户访问测试会话

---

## 13. 当前开发中的关键结论

### 13.1 关于世界书

世界书不是独立系统，而是角色资产的一部分。

### 13.2 关于版本管理

版本管理应该做成**整角色级**，不仅仅是世界书版本。

### 13.3 关于编辑体验

编辑体验要在 Misskey 原有风格上，向“高密度、快编辑、少跳转”方向优化。

### 13.4 关于运行逻辑

世界书与风格不能只存在于界面上，必须真实进入 prompt 拼装和消息链路。

---

## 14. 建议的后续开发任务清单

### 14.1 后端

- 补历史回滚接口
- 补世界书条目触发命中逻辑
- 补世界书条目优先级与作用域选择逻辑
- 为角色 / 风格保存增加更明确的版本元数据
- 继续补角色 / 风格版本 diff 数据结构
- 继续把世界书命中结果接入前端预览

### 14.2 前端

- 把角色版本管理区变成可操作列表
- 把世界书区变成更完整的编辑器
- 增加命中预览
- 增加版本 diff 预览
- 增加回滚确认流程

### 14.3 运维 / 管理

- 确认 Meta 配置在管理员页可用
- 确认模型 Base URL 的安全校验与错误提示
- 确认大文本字段迁移和索引没有问题

---

## 15. 维护说明

本文档应该在以下情况下更新：

- 新增重要 API
- 改变版本管理策略
- 改变世界书存储策略
- 改变会话模型
- 改变 prompt 拼装层级
- 改变编辑器主结构

---

## 16. 与本次开发相关的当前状态总结

本轮开发已经完成了最重要的基础工程：

- 世界书被并入角色系统
- 风格与角色都开始版本化
- 对话请求链路已经开始真正使用风格 directive
- 编辑页已经按工作台思路重构
- 角色 / 风格已补齐 `rollback` 后端接口，草稿回滚闭环开始成立

这意味着项目已经不是“设计中”，而是进入了“功能逐步成型”的阶段。

### 16.1 已完成补充

- 角色快照恢复开始包含世界书与草稿版本号
- 角色 / 风格分别拥有可调用的回滚接口
- 角色 / 风格都补齐了版本列表接口
- 版本相关能力从“仅保存快照”推进到“能恢复当前编辑态”
- 角色编辑页已接入真实版本列表接口并支持从发布快照回滚
- 风格编辑页已接入真实版本列表接口并支持从发布快照回滚确认流程
- 角色 / 风格均新增后端 diff 接口，编辑页可展示草稿与发布版字段差异摘要
- 角色编辑页新增世界书命中预览，支持 `keyword` / `always` 与 `manual` 的实际命中反馈
- 世界书运行时选择逻辑补强：按触发方式分桶限额（always/manual/keyword）并保留优先级排序
- 补齐数据库迁移 `1773200000000-AgentWorldbookAndDraftRevision`：为 `agent_character` 增加 `worldbook`(jsonb) 与 `draftRevision`，为 `agent_dialogue_style` 增加 `draftRevision`，消除新部署运行时缺列风险
- 修复「审核中更新泄漏」：角色 / 风格提交审核时不再覆盖已批准的 `publishedSnapshot`，广场卡片与社区会话在审核期间继续使用上一已上线版本，待通过后由 `review/resolve` 重建快照
- 发布「无变更检测」纳入世界书：`isCharacterContentUnchangedFromPublished` 通过稳定序列化（按条目 id / 关键词排序）比对世界书，避免世界书改动被误判为无变更
- 统一世界书注入策略：世界书不再整段写入 system，改为完全经由最新 user 的 `<runtime-directive>` 的 `<active-worldbook>` 按本轮命中注入；system 仅保留 `<worldbook_protocol>` 协议说明作为安全网，消除 system 与 directive 的重复注入
- Token 预算修正：`buildSendPathBudgets` 按「全部已启用世界书条目」保守预扣 directive 长度（上界），与世界书曾整段写入 system 时的预留量一致，避免上下文溢出回归
- 角色 / 风格编辑页保存成功后自动刷新版本列表与 diff，无需手动刷新页面
- 重新生成 misskey-js API 类型，移除角色 / 风格编辑页中 `versions` / `diff` / `rollback` / `worldbook-match-preview` 调用的 `(misskeyApi as any)` 临时断言，恢复端到端类型安全
- 世界书命中预览支持对「未保存草稿」求值：`worldbook-match-preview` 新增可选 `worldbook` 入参，编辑页直接传当前表单世界书，无需先落库；`selectWorldbookEntriesForPrompt` 放宽入参为结构化对象
- 角色 / 风格编辑页 diff 升级为「逐字段可展开 + 行级 +/- 对比」视图（基于 LCS 逐行 diff），世界书字段展开前先 pretty-print 便于阅读
- 角色编辑页世界书命中预览新增 manual 条目「一键插入 `[[wb:标题]]`」快捷交互
- 真历史版本存储落地：新增 `agent_published_version` 归档表（迁移 `1773300000000`）与实体 / 仓库注册；审核通过（resolve approve）时写入不可变版本快照；`versions` 接口返回真实历史版本列表（含 `isHistorical`）；`rollback` 接口新增可选 `version` 参数，支持回滚到任意历史已上线版本；编辑页可逐版本回滚

### 16.2 下一步建议任务

- 历史版本归档表目前每次审核通过追加一条，长期可考虑保留上限 / 清理策略（接口已限制返回最近 50 条）
- 社区会话场景下的 manual 触发交互（当前仅在编辑页预览处提供，社区用户因不可见世界书条目暂不提供快捷插入）
- diff 行级对比可进一步做「字符级高亮」（当前为行级 +/-）

---

*文档更新建议：每完成一轮结构性功能后，在这里追加「已完成」与「后续任务」条目，避免信息只散落在 commit 和代码里。*
